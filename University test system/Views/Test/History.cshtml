@model IEnumerable<University_test_system.Models.Attempt>

@{
    ViewData["Title"] = "Історія проходження тестів";
}

<div class="knu-page-header">
    <div class="container">
        <h1><i class="bi bi-clock-history me-2"></i>Історія проходження тестів</h1>
        <p>Ваші попередні спроби та результати тестів</p>
    </div>
</div>

@if (!Model.Any())
{
    <div class="text-center py-5">
        <div class="display-1 text-muted mb-3">
            <i class="bi bi-journal-x"></i>
        </div>
        <h4>Немає пройдених тестів</h4>
        <p class="text-muted">На даний момент немає тестів, які Ви пройшли</p>
            
        @if (User.IsInRole("Admin"))
        {
            <a href="/Admin/CreateTest" class="btn btn-knu mt-3">
                <i class="bi bi-plus-circle me-2"></i>Створити тест
            </a>
        }
    </div>
}
else
{
    <div class="min-vh-50 d-flex align-items-center justify-content-center py-5">
        <table class="knu-table">
            <thead>
            <tr>
                <th class="text-center">Назва тесту</th>
                <th class="text-center">Предмет</th>
                <th class="text-center">Дата спроби</th>
                <th class="text-center">Номер спроби</th>
                <th class="text-center">Бали</th>
                <th class="text-center">Дії</th>
            </tr>
            </thead>
            <tbody class="knu-table tbody tr">
            @foreach (var attempt in Model)
            {
                <tr>
                    <td class="text-center align-middle">@attempt.Test.Title</td>
                    <td class="text-center align-middle">@attempt.Test.Subject?.Name</td>
                    <td class="text-center align-middle">@attempt.AttemptDate.ToString("dd.MM.yyyy HH:mm")</td>
                    <td class="text-center align-middle">@attempt.AttemptsCount</td>
                    <td class="text-center align-middle">@attempt.Score</td>
                    <td class="text-center align-middle">
                        <div class="d-flex flex-column gap-1">
                            <a asp-action="Result" asp-route-id="@attempt.Id" class="btn btn-knu btn-sm">
                                Переглянути результат
                            </a>
                            <a asp-action="Leaderboard" asp-route-testId="@attempt.TestId" class="btn btn-knu btn-sm">
                                Переглянути список лідерів
                            </a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}